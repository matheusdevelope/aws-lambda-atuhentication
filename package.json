{
  "scripts": {
    "build":"sam build",
    "pub":"sam build && sam deploy"
  },
  "dependencies": {
    "aws-sdk": "^2.1382.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.115",
    "@types/node": "^20.2.3",
    "typescript": "^5.0.4"
  }
}
